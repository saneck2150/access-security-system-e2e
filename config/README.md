(autogenerated file with dataflow description)
# config

Runtime configuration for the access-security system.

## Files

| File | Purpose |
|------|---------|
| `access_security.yaml` | Main configuration file |

## Configuration Sections

### frame_handler

Controls frame processing and security policies.

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `anti_replay_enabled` | bool | `true` | Enable sequence-based replay protection |
| `replay_window_size` | int | `256` | Number of sequence numbers to track |
| `max_ct_len` | int | `4096` | Maximum ciphertext length in bytes |
| `max_skew_ms` | int | `120000` | Clock drift tolerance (2 minutes) |
| `allow_previous_key_version` | bool | `true` | Accept frames from key version N-1 |
| `enforce_reader_door_binding` | bool | `true` | Verify reader is bound to door |

### key_management

Key derivation and versioning settings.

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `current_key_version` | int | `1` | Active key version for encryption |
| `allow_previous_key_version` | bool | `true` | Allow decryption with previous version |
| `master_key_path` | string | `secrets/master_key.hex` | Path to hex-encoded master key |

### storage

Database configuration.

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `sqlite_path` | string | `data/access.db` | Path to SQLite database |

### admin

HTTP admin API settings.

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `bind_host` | string | `127.0.0.1` | Network interface to bind |
| `port` | int | `8080` | HTTP server port |
| `admin_token` | string | `""` | Bearer token for authentication |
| `max_upload_bytes` | int | `20000000` | Maximum upload size (~20MB) |
| `max_events` | int | `1024` | Event buffer capacity |
| `decision_mode` | string | `"remote"` | `"local"` = in-process, `"remote"` = POST frame to port+1 |
| `decision_url` | string | `"http://127.0.0.1:8081"` | DecisionService host (port 8081 = main port + 1) |

**Note:** The server always starts two HTTP listeners: port 8080 (admin API, hw endpoint) and port 8081 (DecisionService). In `"remote"` mode, `hw_service` POSTs the AEAD frame to port 8081 via HTTP. SQLite uses WAL mode + 5s busy_timeout for concurrent access.

## Usage

The application loads configuration at startup:

```bash
./access_security --config config/access_security.yaml
```

## Production Recommendations

| Setting | Development | Production |
|---------|-------------|------------|
| `admin_token` | `"admin"` | Strong random token |
| `bind_host` | `"0.0.0.0"` | `"127.0.0.1"` behind reverse proxy |
| `master_key_path` | File path | Secrets manager or HSM |

## Environment Variable Overrides

For sensitive values in production, consider extending the config loader to support environment variable overrides:

```bash
export ACCESS_ADMIN_TOKEN="$(openssl rand -hex 32)"
export ACCESS_MASTER_KEY_PATH="/run/secrets/master_key.hex"
``` 