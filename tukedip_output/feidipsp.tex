% !TeX encoding=utf-8
\documentclass[a4paper]{feidipsp}
\usepackage[pdftex]{graphicx}
\DeclareGraphicsExtensions{.pdf,.png,.jpg}
\graphicspath{{figures/}{plots/}}

\usepackage[slovak]{babel}
\providecommand{\uv}[1]{\quotedblbase#1\textquotedblleft}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

\usepackage{amsmath,amsfonts,amssymb,latexsym}
\usepackage{listings}
\usepackage{xcolor}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{blue!70!black}\bfseries,
  commentstyle=\color{gray!70!black}\itshape,
  stringstyle=\color{green!50!black},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=6pt,
  frame=single,
  framesep=4pt,
  rulecolor=\color{gray!50},
  breaklines=true,
  showstringspaces=false,
  tabsize=2,
  captionpos=b,
  xleftmargin=0.5em,
  xrightmargin=0.5em
}

\def\figurename{Obrázok}
\def\tabname{Tabuľka}

%% Allow more flexibility in line breaking (long \texttt{} identifiers, etc.)
\emergencystretch=2em
\tolerance=1500
\hyphenpenalty=50

\usepackage{float}          % [H] float placement
\usepackage{needspace}      % \needspace{Nlines} keeps paragraph+table together

\usepackage[pdftex,colorlinks,citecolor=magenta,bookmarksnumbered,unicode,%
  pdftoolbar=true,pdfmenubar=true,pdfwindowui=true,bookmarksopen=true]{hyperref}
\hypersetup{%
  pdfcreator={pdfLaTeX},
  pdfkeywords={kontrola prístupu, RFID, NFC, AEAD, ChaCha20-Poly1305, systémová príručka},
  pdftitle={Systémová príručka --- Access-Security},
  pdfauthor={Oleksandr Mykhailyshyn},
  pdfsubject={Systémová príručka}
}

\usepackage[numbers,sort&compress]{natbib}
\renewcommand*{\bibsection}{\section{Zoznam použitej literatúry}}

%% ====================================================================
%%  METADATA
%% ====================================================================
\katedra{počítačov a informatiky}
\department{Department of Computers and Informatics}
\odbor{Informatika}
\autor{Oleksandr Mykhailyshyn}
\veduci{doc.~Ing.~Eva Chovancová, PhD.}
%\konzultant{}
\nazov{Zvýšenie bezpečnosti vybraných šifrovacích algoritmov v~prístupových systémoch}
\kratkynazov{Zvýšenie bezpečnosti vybraných šifrovacích algoritmov v~prístupových systémoch}
\nazovprogramu{ACCESS-SECURITY}
\klucoveslova{kontrola prístupu, RFID, NFC, AEAD, ChaCha20-Poly1305, HMAC, HKDF, anti-replay, audit log}
\title{Enhancing Security of Selected Encryption Algorithms in Access Control Systems}
\keywords{access control, RFID, NFC, AEAD, ChaCha20-Poly1305, HMAC, HKDF, anti-replay, audit log}
\datum{24.04.2026}


\begin{document}
\bibliographystyle{plainnat}

\titulnastrana

\tableofcontents

\newpage

\listoftables

\newpage

\setcounter{page}{1}

\section{Funkcia programu}

\emph{Access-Security} je prototyp systému kontroly fyzického prístupu na báze RFID/NFC. Jeho úlohou je autorizovať držiteľov kariet na prechod cez elektronicky riadené dvere na základe kombinácie roly karty a~role dverí, pričom celý komunikačný reťazec medzi čítačkou a~serverom je chránený kryptograficky --- autentifikovaným šifrovaním (AEAD)\cite{RFC8439}, anti-replay ochranou, HKDF per-reader deriváciou kľúčov\cite{RFC5869}, detekciou protokolových anomálií\cite{NIST80094} a~tamper-evident auditom\cite{SchneierKelsey1999}.

Systém tvoria tri hlavné časti:

\begin{itemize}
\item \textbf{Hardvérová čítačka} postavená na mikrokontroléri ESP32\cite{Espressif2024} a~module RC522, komunikujúca podľa ISO~14443\cite{ISO14443}. Číta UID karty, pripojí identifikátor čítačky a~dverí, doplní monotónne počítadlo \texttt{hw\_seq} a~cez bezdrôtové pripojenie Wi-Fi odošle autentifikovanú HTTP požiadavku (HMAC-SHA-256\cite{FIPS1981} podpis nad reťazcom \texttt{"POST /api/hw/uid\textbackslash n"} $+$ telo požiadavky) na server.
\item \textbf{Serverová aplikácia} v~jazyku C++20, pozostávajúca z~dvoch nezávislých HTTP služieb: \emph{frontend} služba overuje HMAC podpis požiadavky a~konštruuje interný AEAD frame, \emph{DecisionService} frame dešifruje, overí anti-replay, spustí anomálny detektor, vyhodnotí autorizáciu (RBAC) a~výsledok zapíše do auditného logu. Lokálne úložisko je realizované cez SQLite\cite{SQLiteSec} v~WAL móde.
\item \textbf{Webové administrátorské rozhranie} pre správu čítačiek, kariet, rolí dverí, kontrolu auditného logu s~overením integrity reťaze a~sledovanie prevádzkových udalostí v~reálnom čase.
\end{itemize}

Program je určený na nasadenie v~prostredí, kde konvergujú požiadavky fyzickej a~informačnej bezpečnosti --- tam, kde sa od prístupového systému očakáva ochrana nielen proti jednoduchému skopírovaniu karty, ale aj proti aktívnym útokom na komunikačný kanál (replay, manipulácia hlavičky, podsunutie nonce, rollback sekvenčného čísla, forgery pokusy o~AEAD tag a~pod.).

\section{Analýza riešenia}

Detailný teoretický rozbor problematiky --- analýza aktuálnych prístupov ku kontrole fyzického prístupu, model hrozieb (STRIDE\cite{STRIDE}), porovnanie kryptografických primitív (ChaCha20-Poly1305\cite{RFC8439} vs.\ AES-GCM, stratégie generovania nonce, HKDF derivácia kľúčov, HMAC autentifikácia), zásady návrhu bezpečných protokolov, ochrana súkromia identifikátorov, tamper-evident auditovateľnosť, bezpečnosť vstavaného zariadenia a~bezpečnosť transportnej vrstvy --- je obsahom kapitoly 2 \emph{Analýza} hlavnej diplomovej práce, ktorá je súčasťou rovnakého priloženého média.

Táto systémová príručka sa zameriava na technickú dokumentáciu konkrétnej implementácie: organizáciu kódovej bázy, algoritmy realizované vlastným kódom, údajové štruktúry, konfiguráciu a~spôsob zostavenia a~spustenia systému. Pre úplné odôvodnenie zvolených kryptografických primitív a~architektonických rozhodnutí je čitateľ odkázaný na hlavnú diplomovú prácu.

\section{Popis programu}
\label{sec:popis}

Program pozostáva z~troch vrstiev. Hardvérová vrstva \texttt{hw\_layer} je firmvér bežiaci na ESP32 s~čítačkou MFRC522; jeho úlohou je čítanie UID karty a~odoslanie autentifikovanej HTTP požiadavky serveru. Serverová časť je písaná v~jazyku C++20 a~je rozdelená na dve nezávislé HTTP služby: \emph{frontend} (port 8080), ktorá prijíma požiadavky od čítačiek, overuje HMAC podpis a~konštruuje interný AEAD frame, a~\emph{DecisionService} (port 8081), ktorá frame dešifruje, spúšťa anti-replay kontrolu, anomálny detektor a~RBAC vyhodnotenie a~zapisuje výsledok do auditného logu. DecisionService počúva výhradne na loopback rozhraní \texttt{127.0.0.1}, takže je dostupný len lokálnej frontend službe. Webové administrátorské rozhranie slúži pre správu čítačiek, kariet a~rolí dverí a~pre sledovanie prevádzky v~reálnom čase.

Rozhodovacia cesta (\texttt{DecisionEngine::handleFrameBytes}) je organizovaná od najlacnejších kontrol po najdrahšie, aby sa neplatné požiadavky odfiltrovali čo najskôr: parsovanie $\rightarrow$ validácia čítačky $\rightarrow$ kontrola verzie kľúča $\rightarrow$ anti-replay (read-only) $\rightarrow$ AEAD dešifrovanie $\rightarrow$ anomálny detektor $\rightarrow$ RBAC $\rightarrow$ audit.

\subsection{Popis riešenia}
\label{sec:popis-riesenia}

Projekt sa skladá z~troch komponentov, ktoré dokopy tvoria funkčný prístupový systém spolu s~experimentálnou platformou pre jeho vyhodnotenie. Bezpečnostné mechanizmy implementované v~serverovej časti (AEAD frame s~AAD väzbou, HKDF per-reader derivácia kľúčov, deterministický nonce, anti-replay sliding window, detektor protokolových anomálií s~karanténou, tamper-evident audit log a~pseudonymizácia identifikátorov kariet) sú podrobne popísané a~odôvodnené v~kapitole 3 hlavnej diplomovej práce; tu sú spomenuté len v~rozsahu potrebnom pre orientáciu v~kóde.

\paragraph{Hardvérová vrstva.} Firmvér pre ESP32 s~čítačkou MFRC522, písaný pre Arduino-ESP32 framework. Číta UID karty, udržuje monotónne počítadlo \texttt{hw\_seq} v~perzistentnom NVS úložisku, a~odošle autentifikovanú HTTP požiadavku cez Wi-Fi (HMAC-SHA-256 podpis nad reťazcom \texttt{"POST /api/hw/uid\textbackslash n"} $+$ telo požiadavky). Detailné zapojenie a~popis firmvéru je v~následných podkapitolách.

\paragraph{Serverová aplikácia.} Dve nezávislé HTTP služby v~jazyku C++20: \emph{frontend} (port 8080) prijíma hardvérové požiadavky, overuje HMAC podpis a~konštruuje interný AEAD frame; poskytuje aj administratívne API a~servuje webové rozhranie. \emph{DecisionService} (port 8081, viazaný na loopback) frame dešifruje, spúšťa pipeline kontrol (anti-replay, anomálny detektor, RBAC, audit) a~vracia rozhodnutie. Úložisko je SQLite v~režime WAL.

\paragraph{Webové administrátorské rozhranie.} Vanilla JavaScript (bez frameworku) servovaný frontend službou. Spravuje čítačky, karty a~role dverí, verifikuje integritu auditného logu a~sleduje prevádzkové udalosti v~reálnom čase (polling každých 500\,ms; auditný log auto-refresh každú sekundu).

\paragraph{Experimentálna a~analytická vrstva.} C++ harness (\texttt{experiments/}) zdieľa produkčný kód kryptografickej a~rozhodovacej vrstvy, čím sa zaručuje, že merania odrážajú skutočné správanie systému. Obsahuje sedem útočných tried (S1--S7), pričom S3 je rozdelené na dve samostatné binárky (S3a: \texttt{s3\_rng\_fault}, S3b: \texttt{s3\_cross\_reader}) --- spolu teda osem spustiteľných programov a~osem CSV súborov. Python skripty (\texttt{analysis/}) spracúvajú CSV výstupy a~generujú grafy a~LaTeX-ready tabuľky pomocou \texttt{pandas}, \texttt{matplotlib} a~\texttt{seaborn}.

\paragraph{Členenie zdrojového archívu.} Priložené médium obsahuje produkčný kód systému spolu s~experimentálnou a~analytickou vrstvou. Moduly \texttt{experiments/} a~\texttt{analysis/} (spolu s~adresárom \texttt{tukedip\_output/}) sú čisto aditívne --- nijako nemenia správanie produkčného systému, iba dopĺňajú nástroje pre meranie a~generovanie grafov. Tým je zaručené, že experimenty bežia na presne tom istom kóde, aký by bol nasadený v~produkcii. Pre minimálnu produkčnú distribúciu postačuje odstrániť adresáre \texttt{experiments/}, \texttt{analysis/} a~\texttt{tukedip\_output/} --- produkčný server sa zostaví aj bez nich bez úpravy koreňového \texttt{CMakeLists.txt}.

\subsection[Popis algoritmov a~údajových štruktúr, globálnych premenných]{Popis algoritmov a~údajových štruktúr, globálnych\\ premenných}

Táto podkapitola opisuje vlastné algoritmy implementované v~projekte, kľúčové údajové štruktúry, rozdelenie kódu na moduly a~model interakcie s~používateľom.

\paragraph{Modulárna štruktúra.}

Serverová časť je rozdelená do deviatich modulov so statickými knižnicami a~jednosmernými závislosťami: \texttt{crypto\_lib} (AEAD, HKDF, generátory nonce, HMAC), \texttt{protocol\_lib} (binárny formát frame, replay window), \texttt{key\_manager} (derivácia kľúčov, rotácia), \texttt{access\_core} (\texttt{FrameHandler}, \texttt{ProtocolAnomalyDetector}), \texttt{access\_decision} (\texttt{DecisionEngine}, RBAC), \texttt{access\_storage} (SQLite, audit log), \texttt{config\_loader} (YAML), \texttt{runtime\_events} (udalosti pre web) a~\texttt{access\_admin} (HTTP server, služby, web frontend). Všetky moduly sú pokryté testami --- buď priamo cez vlastnú sadu v~podadresári \texttt{tests/}, alebo nepriamo prostredníctvom testov závislých modulov, ktoré ich využívajú.

\paragraph{Algoritmus: rozhodovacia pipeline.}

Hlavný algoritmus serverovej časti je obsiahnutý v~\texttt{DecisionEngine::handleFrameBytes} (a~podriadenej \texttt{FrameHandler::handle}). Spracovanie frame prebieha v~pevne definovanom poradí od najlacnejších kontrol po najdrahšie, aby sa neplatné požiadavky odfiltrovali čo najskôr:

\begin{enumerate}
\item \textbf{Parsovanie frame} --- validuje magic, verziu, hlavičku, ciphertext (s~kontrolou maximálnej dĺžky pred alokáciou), tag a~absenciu trailing bajtov.
\item \textbf{Validácia čítačky a~verzie kľúča} --- overenie, že \texttt{reader\_id} je registrovaný a~\texttt{key\_version} je aktuálna alebo (pri povolenej tolerancii) bezprostredne predchádzajúca.
\item \textbf{Anti-replay kontrola} --- \emph{read-only} volanie \texttt{ReplayWindow::contains(seq)}; pri nájdení duplicitu sa frame odmietne bez pokusu o~dešifrovanie.
\item \textbf{AEAD dešifrovanie} --- derivácia per-reader kľúča cez HKDF, overenie Poly1305 tagu nad hlavičkou (AAD) a~ciphertextom.
\item \textbf{Zaznamenanie sekvencie} (\texttt{remember}) --- až po úspešnom dešifrovaní sa \texttt{seq} pridá do replay window, aby neplatné frame nemohli \uv{otráviť} stav.
\item \textbf{Anomálny detektor} --- pri úspešnom dešifrovaní vyhodnotenie štyroch protokolových invariantov (viď~nižšie).
\item \textbf{RBAC autorizácia} --- vyhľadanie karty podľa HMAC odtlačku, overenie väzby čítačka $\leftrightarrow$ dvere a~povolenia role pre cieľové dvere.
\item \textbf{Zápis do auditného logu} --- bez ohľadu na výsledok rozhodnutia (allow alebo deny).
\end{enumerate}

\paragraph{Hlavné implementačné algoritmy.}
\texttt{ReplayWindow} kombinuje \texttt{std::deque} (FIFO eviction) a~\texttt{std::unordered\_set} (kontrola $O(1)$) plus ukazovateľ \texttt{\_maxSeen}; tolerujú sa krátke preusporiadania, odmietajú sa duplikáty a~hodnoty staršie ako \texttt{maxSeen $-$ windowSize}\cite{RFC5116}. \texttt{HmacNonceGenerator} odvodzuje nonce ako prvých 12, resp.\ 24 bajtov výstupu HMAC-SHA-256 nad serializovaným kontextom hlavičky\cite{RFC5869}; prijímateľ prepočíta hodnotu a~porovná v~konštantnom čase cez \texttt{sodium\_memcmp}\cite{LibsodiumDoc}. \texttt{ProtocolAnomalyDetector} udržuje per-reader stav a~zakaranténuje čítačku pri replay, nonce mismatch, sekvenčnom rollbacku alebo \texttt{tag\_fail\_streak}\cite{DefenseInDepth,NIST80094}. \texttt{SqliteAuditLog} reťazí záznamy cez \texttt{HMAC(K\_audit, prev\_hash $\|$ fields)} a~paralelne aktualizuje \texttt{audit\_anchor}\cite{SchneierKelsey1999}. \texttt{CardIdHasher} ukladá UID len ako HMAC s~pepperom; pri rotácii sa hľadanie skúša s~aktuálnym aj predchádzajúcim pepperom. Parser binárneho frame validuje magic \texttt{AS01}, verziu, dĺžku ciphertextu (limit pred alokáciou) a~absenciu trailing bajtov; každá neplatná pozícia končí výnimkou s~kódom chyby. Detailné matematické formulácie a~odôvodnenia sú v~kapitole~3 hlavnej diplomovej práce.

\paragraph{Konfigurácia.}

Behová konfigurácia je udržiavaná v~YAML súboroch v~adresári \texttt{config/}. Hlavný produkčný profil \texttt{access\_security.yaml} obsahuje štyri skupiny parametrov (\texttt{frame\_handler}, \texttt{key\_management}, \texttt{storage}, \texttt{admin}). Šesť experimentálnych profilov \texttt{profile\_a\{1,2\}\_r\{0,1,2\}.yaml} tieto sekcie dopĺňa o~piatu skupinu \texttt{experiment} (cipher mode, nonce mode, detektor), čím kombinujú dva AEAD algoritmy a~tri stratégie generovania nonce.

Modul \texttt{config\_loader} poskytuje funkciu \texttt{loadFromYaml(path)} $\rightarrow$ \texttt{Config}, ktorá načíta YAML a~validuje hodnoty. Výsledná štruktúra \texttt{Config} agreguje päť podštruktúr: \texttt{FrameHandlerConfig} (anti-replay, max skew, väzby), \texttt{StorageConfig} (cesta k~SQLite), \texttt{AdminConfig} (HTTP port, admin token, HMAC secret, decision\_mode), \texttt{KeyManagementYaml} (verzia kľúča, cesta k~master key) a~\texttt{ExperimentConfig} (AEAD cipher, nonce stratégia, detektor, kotva auditu). Všetky polia majú rozumné predvolené hodnoty, takže konfigurácia je priamočiara a~zrozumiteľná bez dokumentácie.

\paragraph{Schéma databázy.}

Úložisko je realizované cez SQLite v~režime WAL. Schéma obsahuje šesť tabuliek (tabuľka~\ref{t:db-schema}), ktoré sú pri prvom spustení automaticky vytvorené modulom \texttt{access\_storage} cez \texttt{CREATE TABLE IF NOT EXISTS}.

\begin{table}[!ht]\caption{Schéma SQLite databázy}\label{t:db-schema}
\smallskip
\centering
\begin{tabular}{|l|l|} \hline
\textbf{Tabuľka} & \textbf{Obsah} \\ \hline
\texttt{cards}         & HMAC(pepper, UID) $\rightarrow$ rola karty \\ \hline
\texttt{door\_roles}   & Povolené roly pre každé dvere (RBAC) \\ \hline
\texttt{readers}       & Registrované čítačky s~verziou kľúča \\ \hline
\texttt{reader\_doors} & Väzba čítačka $\leftrightarrow$ dvere (FK, CASCADE) \\ \hline
\texttt{audit\_log}    & Záznamy auditu s~HMAC hash chain \\ \hline
\texttt{audit\_anchor} & Ochranná kotva proti truncation \\ \hline
\end{tabular}
\end{table}

Žiadna tabuľka neobsahuje surové UID kariet --- ukladá sa len HMAC odtlačok s~pepperom. Tabuľky \texttt{audit\_log} a~\texttt{audit\_anchor} spolu tvoria tamper-evident auditný mechanizmus opísaný v~podkapitole~\ref{sec:popis-riesenia}.

\paragraph{Globálne premenné.}

Projekt sa zámerne vyhýba globálnym mutable premenným. Jediný agregujúci objekt serverovej aplikácie je inštancia \texttt{AppState} (v~module \texttt{access\_admin}), ktorá drží zdieľaný stav --- \texttt{DecisionEngine}, \texttt{KeyManager}, úložisko, kruhový buffer prevádzkových udalostí --- a~dva mutexy: \texttt{app.m} pre administratívne API a~\texttt{app.engineMutex} pre rozhodovaciu cestu. Všetky modifikácie zdieľaného stavu sú chránené jedným z~týchto mutexov. Firmvérová časť udržuje len statické premenné v~rámci hlavného skechu (\texttt{hwSeq}, \texttt{lastUid}, \texttt{lastSendMs}) --- plne opísané sú v~následnej podkapitole o~hardvérovej vrstve.

\paragraph{Dialóg s~používateľom.}

Interakcia s~administrátorom prebieha výhradne cez webové rozhranie: používateľ v~prehliadači otvorí administrátorskú stránku (servovanú frontend službou na porte 8080), zadá voliteľný administrátorský token a~prepína medzi sekciami \emph{Readers}, \emph{Door roles}, \emph{Cards}, \emph{Audit}, \emph{Runtime logs} a~\emph{DB Export/Import}. Každá operácia (pridanie karty, zrušenie roly, verifikácia auditu) je realizovaná volaním REST endpointu; výsledok je zobrazený v~zodpovedajúcej tabuľke alebo stavovom paneli. Dialóg s~používateľom karty (t.\,j.~samotné priloženie karty k~čítačke) je realizovaný cez svetelnú a~zvukovú signalizáciu opísanú v~následnej podkapitole o~hardvérovej vrstve.

\subsection{Popis modulov a~podprogramov}

Táto podkapitola sumarizuje verejné rozhranie každého modulu --- najdôležitejšie triedy a~funkcie, ich vstupno-výstupné parametre a~vzťah k~ostatným modulom. Globálne premenné jednotlivé moduly nepoužívajú; všetok zdieľaný stav je centralizovaný v~objekte \texttt{AppState} (popísaný v~3.2). Poradie popisu zodpovedá závislostiam: od základných knižníc po vyššie vrstvy.

\paragraph{\texttt{hw\_layer} (firmvér ESP32).}
Súbor \texttt{hw\_layer.ino} obsahuje obvyklú dvojicu Arduino funkcií. \texttt{setup()} vykoná inicializáciu (sériová konzola, GPIO, LEDC, NVS, SPI, MFRC522, Wi-Fi). \texttt{loop()} periodicky volá \texttt{rfid.PICC\_IsNewCardPresent()} a~\texttt{rfid.PICC\_ReadCardSerial()}; pri detekcii karty zavolá \texttt{postUidToServer(uid, allow)}. Tá overí Wi-Fi, inkrementuje \texttt{hw\_seq}, zostaví JSON telo, vypočíta HMAC podpis (\texttt{hmacSha256Hex}) a~odošle požiadavku cez \texttt{HTTPClient}. Výsledok je signalizovaný cez \texttt{signalAllow()} alebo \texttt{signalDeny()} (LED + LEDC tón).

\paragraph{\texttt{crypto\_lib}.}
Trieda \texttt{SecureAead} (\texttt{secure\_aead.hpp}) zapuzdruje ChaCha20-Poly1305 alebo XChaCha20-Poly1305 z~knižnice libsodium. Verejné šifrovacie metódy: \texttt{sealWithSeq(plaintext, aad, seq)} (interne odvodí nonce zo \texttt{seq}) a~\texttt{sealWithNonce(plaintext, aad, nonce)} (explicitne dodaný nonce) --- obe vracajú \texttt{Ciphertext} obsahujúci ciphertext, tag a~nonce. Dešifrovanie je metódou \texttt{openWithNonce(ct, tag, aad, nonce)}, ktorá vracia \texttt{std::vector<uint8\_t>} s~plaintextom alebo vyhodí výnimku pri zlyhaní tag verifikácie. Trieda \texttt{Hkdf} (\texttt{hkdf.hpp}) implementuje HKDF-SHA-256 podľa RFC~5869: konštruktor vykoná extract fázu (\texttt{PRK = HMAC(salt, IKM)}), metóda \texttt{derive() $\rightarrow$ vector<uint8\_t>} expand fázu. Generátory nonce (\texttt{nonce\_generator.hpp}) implementujú spoločné rozhranie \texttt{INonceGenerator::generate(context, seq) $\rightarrow$ array<uint8\_t,24>}; konkrétne triedy \texttt{RandomNonceGenerator} (libsodium \texttt{randombytes\_buf}) a~\texttt{HmacNonceGenerator} (HMAC nad kontextom).

\paragraph{\texttt{protocol\_lib}.}
\texttt{Frame} (\texttt{frame.hpp}) je dátová štruktúra obsahujúca \texttt{Header}, ciphertext (\texttt{vector<uint8\_t>}) a~\texttt{Tag16}. Voľná funkcia \texttt{serialize(const Frame\&) $\rightarrow$ vector<uint8\_t>} vyprodukuje binárny tvar (magic, verzia, hlavička, dĺžka ciphertext, ciphertext, tag); \texttt{parseFrame(bytes, maxCtLen=4096) $\rightarrow$ Frame} robí opačnú operáciu cez triedu \texttt{FrameParser} s~ochranou proti DoS útoku. Trieda \texttt{ReplayWindow} (\texttt{replay\_window.hpp}) poskytuje \texttt{contains(seq) $\rightarrow$ bool}, \texttt{remember(seq)} a~\texttt{accept(seq) $\rightarrow$ bool} (atomická kontrola $+$ vloženie).

\paragraph{\texttt{key\_manager}.}
Trieda \texttt{KeyManager} (\texttt{key\_manager.hpp}) odvodzuje podkľúče z~master kľúča. Konštruktor prijíma 32-bajtový \emph{MasterKey} a~\emph{KeyManagerConfig} (aktuálna verzia, povolenie predošlej verzie). Verejné metódy:
\begin{itemize}
\item \texttt{deriveAeadKey(readerId, keyVersion)} --- HKDF AEAD kľúč so saltom obsahujúcim ID čítačky a~verziu,
\item \texttt{deriveNonceKey(readerId, keyVersion)} --- per-reader kľúč pre HMAC nonce konštrukciu,
\item \texttt{deriveAuditHmacKey()} --- per-inštanciu kľúč pre auditný hash chain (bez parametrov),
\item \texttt{deriveCardPepper(keyVersion)} --- pepper pre pseudonymizáciu kariet,
\item \texttt{isAcceptedKeyVersion(keyVersion)} --- kontrola, či je verzia akceptovaná pre dešifrovanie,
\item \texttt{masterAsAeadKey()} --- priamy režim bez HKDF (pre testovanie).
\end{itemize}

\paragraph{\texttt{access\_core}.}
\texttt{FrameHandler::handle(frameBytes) $\rightarrow$ HandleResult} je centrálna metóda spracovania frame: parsuje, validuje čítačku a~verziu kľúča, volá \texttt{ReplayWindow::contains}, deriváciu kľúča a~AEAD dešifrovanie. \texttt{HandleResult} obsahuje pole \texttt{allow}, \texttt{reason} (string code), \texttt{plaintext} a~\texttt{header}. \texttt{ProtocolAnomalyDetector} (\texttt{protocol\_anomaly\_detector.hpp}) ponúka per-reader päť report metód: \texttt{reportReplay}, \texttt{reportSeq}, \texttt{reportNonceMismatch}, \texttt{reportTagFailure} a~\texttt{reportSuccess}; spolu s~dotaznými \texttt{isQuarantined} a~\texttt{quarantineInfo}. Trieda je \emph{thread-safe} (interný mutex).

\paragraph{\texttt{access\_decision}.}
Hlavná metóda \texttt{handleFrameBytes} (s~parametrami \texttt{frameBytes} a~mapou replay window podľa čítačky) vracia \texttt{DecisionResult} a~orchestruje úplný rozhodovací cyklus: volá \texttt{FrameHandler::handle}, vyhodnotí stav anomálneho detektora, prevedie pseudonymizáciu UID cez \texttt{CardIdHasher::hmacHex}, vykoná RBAC vyhľadanie cez \texttt{IAccessStore}, zapíše do \texttt{IAuditLog} a~vyšle udalosť do voliteľnej \texttt{EventBus}. Štruktúra \texttt{DecisionResult} obsahuje \texttt{allow}, \texttt{reason} (kódy ako \texttt{ok}, \texttt{replay}, \texttt{decrypt\_failed}, \texttt{unknown\_card}, \texttt{forbidden}, \texttt{quarantined}). \texttt{CardIdHasher::hmacHex(uid) $\rightarrow$ string} vypočíta hex HMAC s~pepperom.

\paragraph{\texttt{access\_storage}.}
\texttt{SqliteAccessStore} implementuje rozhranie \texttt{IAccessStore} s~CRUD operáciami nad tabuľkami \texttt{readers}, \texttt{cards}, \texttt{door\_roles}, \texttt{reader\_doors}. \texttt{SqliteAuditLog} implementuje \texttt{IAuditLog::append(event)}; interne pri zápise volá \texttt{getLastEntryHash()} a~\texttt{computeEntryHash(prev, event)} pre tvorbu HMAC chain a~aktualizuje tabuľku \texttt{audit\_anchor}. Verifikácia (\texttt{verifyAuditChain(db, key)} v~\texttt{audit\_verify.cpp}) prečíta všetky záznamy v~poradí a~rekonštruuje reťazec; vracia \texttt{AuditVerifyResult \{bool ok, int64\_t bad\_id, std::string error\}} --- pri prvej zistenej nezhode sa verifikácia zastaví a~vráti ID problémového záznamu spolu s~popisom chyby.

\paragraph{Pomocné moduly.}
Modul \texttt{config\_loader} obsahuje jedinú verejnú funkciu \texttt{loadFromYaml(path)}, ktorá načíta YAML súbor cez \texttt{yaml-cpp} a~vráti naplnenú štruktúru \texttt{Config}. \texttt{runtime\_events} poskytuje triedu \texttt{EventBus} s~kruhovým bufferom prevádzkových udalostí (\texttt{push(event)}, \texttt{getAfter(afterId, limit)}) pre real-time zobrazovanie vo webovom rozhraní. \texttt{access\_admin} obsahuje vstupný bod \texttt{main()} (spustí dva HTTP servery), centrálny \texttt{AppState} a~service vrstvu (\texttt{hw\_service}, \texttt{cards\_service}, \texttt{readers\_service}, \texttt{audit\_service} a~ďalšie), ktorá tvorí tenkú adaptáciu medzi HTTP požiadavkami a~jadrom systému.

\paragraph{Webové rozhranie (\texttt{web/}).}
Vanilla JavaScript priamo servovaný frontend službou. Modul \texttt{app.js} obsluhuje navigáciu medzi sekciami a~spúšťa \texttt{startEventPolling()} (interval 500\,ms) a~\texttt{startAuditAutoRefresh()} (interval 1\,s, aktívny iba pri otvorenej audit sekcii). Jednotlivé súbory \texttt{readers.js}, \texttt{cards.js}, \texttt{doors.js}, \texttt{audit.js}, \texttt{db.js} implementujú obslužné funkcie pre svoje sekcie; všetky volajú jednotné API helper \texttt{api(path, opts)} z~\texttt{utils.js}, ktoré pridá \texttt{X-Admin-Token} hlavičku.

\paragraph{\texttt{experiments} (harness a~scenáre).}
Samostatný CMake podprojekt mimo produkčného runtime, ktorý linkuje produkčné knižnice ako spotrebiteľ. Skladá sa zo~statickej knižnice \texttt{experiments\_common} a~ôsmich samostatných spustiteľných programov (\texttt{s1\_replay} -- \texttt{s7\_nonce\_tamper}). Verejné API tvoria: trieda \texttt{ExperimentContext} (\texttt{experiment\_context.hpp}), ktorá pre každú iteráciu inštancuje vlastnú kópiu rozhodovacej pipeline (\texttt{KeyManager}, \texttt{SqliteAccessStore}, \texttt{DecisionEngine}, \texttt{ProtocolAnomalyDetector}) a~vystavuje metódu \texttt{processFrame(frameBytes) $\rightarrow$ DecisionResult} s~voliteľným E2E režimom cez \texttt{setE2eUrl(url)}; abstraktná trieda \texttt{IScenario} v~\texttt{scenario\_runner.hpp} s~metódami \texttt{setup(ctx)} a~\texttt{buildTrialFrame(ctx)}, ktoré každý scenár implementuje; a~voľná funkcia \texttt{runScenario(scenario)} iterujúca cez profily, kroky a~behy. Orchestráciu E2E režimu zabezpečuje shell skript \texttt{run\_e2e.sh}, ktorý pre každý profil generuje dočasný YAML konfig, spúšťa reálny \texttt{access\_admin} server, seeduje databázu cez REST API a~agreguje CSV výstupy.

\paragraph{\texttt{analysis} (Python pipeline pre grafy a~tabuľky).}
Samostatný Python modul mimo CMake buildu, ktorý spracúva CSV výstupy harnessu a~generuje publikačné grafy aj LaTeX tabuľky pre prácu. Vstupným bodom je skript \texttt{analyze.py} s~CLI prepínačmi \texttt{--results-dir} (predvolene \texttt{build/experiments/results}) a~\texttt{--output-dir} (predvolene \texttt{analysis/plots}). Závislosti sú uvedené v~\texttt{requirements.txt}: \texttt{pandas>=2.0} (spracovanie CSV), \texttt{matplotlib>=3.7} a~\texttt{seaborn>=0.13} (grafy), \texttt{numpy>=1.24} (numerické výpočty). Globálne konštanty v~hornej časti skriptu (\texttt{PROFILE\_ORDER}, \texttt{PROFILE\_PALETTE}, \texttt{MODE\_LABELS}, \texttt{ALGO\_LABELS}, \texttt{SCENARIO\_TITLES}) garantujú konzistentné farby, poradie a~popisy profilov naprieč všetkými výstupmi.

Pipeline produkuje dve kategórie výstupov do \texttt{analysis/plots/}: (a)~sedem PNG grafov --- \texttt{attack\_success\_heatmap}, \texttt{attack\_success\_by\_n}, \texttt{quarantine\_speed}, \texttt{latency\_boxplot}, \texttt{throughput}, \texttt{detector\_overhead} a~\texttt{scenario\_summary\_table}, všetky uložené pri 300\,DPI pre tlač; (b)~tri LaTeX tabuľky --- \texttt{scenario\_summary.tex}, \texttt{throughput\_table.tex} a~\texttt{baseline\_correctness.tex}, ktoré sa vkladajú priamo do textu diplomovej práce cez \texttt{\textbackslash input\{plots/...\}}. Tento mechanizmus zabezpečuje, že numerické hodnoty v~tabuľkách sú vždy generované z~najnovších experimentálnych dát a~nemôžu sa \uv{rozísť} s~grafmi.

\paragraph{Tok spracovania požiadavky.}
Tabuľka~\ref{t:request-flow} zachytáva cestu požiadavky od priloženia karty cez tri vrstvy systému (ESP32 $\rightarrow$ frontend $\rightarrow$ DecisionService) až po návrat rozhodnutia firmvéru, vrátane vnútornej štruktúry rozhodovacej pipeline.

\begin{table}[!htbp]
\caption{Tok spracovania požiadavky a~rozhodovacia pipeline}\label{t:request-flow}
\centering
\footnotesize
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{|c|l|p{5.5cm}|} \hline
\textbf{\#} & \textbf{Vrstva / volanie} & \textbf{Účel} \\ \hline
\multicolumn{3}{|c|}{\emph{ESP32 firmvér}} \\ \hline
1 & \texttt{PICC\_ReadCardSerial()} & Detekcia karty, čítanie UID. \\ \hline
2 & \texttt{postUidToServer(uid, allow)} & Inkrementácia \texttt{hw\_seq} (NVS), HMAC podpis, HTTP POST. \\ \hline
\multicolumn{3}{|c|}{\emph{Frontend (port 8080)}} \\ \hline
3 & \texttt{verifyHwSignature(...)} & Overenie HMAC podpisu požiadavky. \\ \hline
4 & \texttt{buildEncryptedFrameBytes(...)} & Nonce, AEAD šifrovanie, serializácia frame. \\ \hline
5 & \texttt{POST /api/decision/frame} $\longrightarrow$ & HTTP POST na loopback \texttt{127.0.0.1:8081}. \\ \hline
\multicolumn{3}{|c|}{\emph{DecisionService (port 8081, loopback) --- \texttt{handleFrameBytes}}} \\ \hline
6 & \texttt{parseFrame(bytes, maxCtLen)} & Magic, verzia, dĺžka (limit 4096\,B), trailing bajty. \\ \hline
7 & \texttt{validateReader / KeyVersion} & Registrácia čítačky a~akceptovaná verzia kľúča. \\ \hline
8 & \texttt{ReplayWindow::contains(seq)} & Anti-replay (read-only). \\ \hline
9 & \texttt{deriveAeadKey + openWithNonce} & HKDF per-reader + AEAD dešifrovanie. \\ \hline
10 & \texttt{ReplayWindow::remember(seq)} & Zápis sekvencie --- až po úspešnom dešifrovaní. \\ \hline
11 & \texttt{isQuarantined / reportSeq} & R2: kontrola karantény a~rollbacku. \\ \hline
12 & \texttt{CardIdHasher + roleForCardHmac} & Pseudonymizácia UID, vyhľadanie role. \\ \hline
13 & \texttt{isAllowed(doorId, role)} & RBAC: väzba dvere $\leftrightarrow$ rola. \\ \hline
14 & \texttt{IAuditLog::append + EventBus} & HMAC hash chain + udalosť pre web. \\ \hline
\multicolumn{3}{|c|}{\emph{Návrat odpovede}} \\ \hline
15 & $\longleftarrow$ \texttt{\{"allow": true|false\}} & JSON odpoveď firmvéru. \\ \hline
16 & \texttt{signalAllow / signalDeny} & Svetelná a~zvuková signalizácia. \\ \hline
\end{tabular}
\end{table}

\paragraph{Vzťah modulov navzájom.}
Smer závislostí v~produkčnom runtime je striktne jednosmerný: \texttt{access\_admin} a~\texttt{access\_decision} sú vyššie vrstvy a~používajú nižšie (\texttt{access\_core}, \texttt{access\_storage}, \texttt{key\_manager}, \texttt{protocol\_lib}, \texttt{crypto\_lib}). Žiadny nižší modul nezávisí od vyššieho, čím je zaručená testovateľnosť každého modulu izolovane (každý má vlastný adresár \texttt{tests/}). \texttt{config\_loader} a~\texttt{runtime\_events} sú \uv{laterálne} pomocné moduly bez závislostí na rozhodovacej ceste. Modul \texttt{experiments} je voči produkčnému runtime paralelný --- linkuje produkčné knižnice ako spotrebiteľ, ale nie je nimi linkovaný; vďaka tomu ich môže používať bez rizika, že sa experimentálny kód dostane do produkčného binárneho výstupu. Modul \texttt{analysis} je úplne oddelený od C++ ekosystému (Python) a~s~ostatnými časťami komunikuje len cez CSV súbory na disku.

\subsection{Popis vstupných a~výstupných súborov}

Táto podkapitola sumarizuje vstupné a~výstupné súbory produkčného systému a~experimentálnej platformy. Produkčné vstupy sú v~tabuľke~\ref{t:prod-inputs}; produkčné výstupy sú popísané v~nasledujúcom odseku. Experimentálna časť (tabuľky~\ref{t:exp-cli}--\ref{t:analysis-outputs}) pokrýva CLI parametre, CSV výstupy harnessu a~výstupy analytického pipeline.

\paragraph{Produkčné vstupy.}
Systém vyžaduje tri vstupné súbory (tabuľka~\ref{t:prod-inputs}): \texttt{config/access\_security.yaml} (jediný súbor upravovaný pri nasadení), \texttt{secrets/master\_key.hex} (jednorázovo generovaný, mimo zdrojového archívu) a~\texttt{hw\_layer/hw\_config.h} (kompiluje sa priamo do firmvéru).

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.15}
\caption{Vstupné súbory produkčného systému}\label{t:prod-inputs}
\begin{tabular}{|l|l|p{5.5cm}|} \hline
\textbf{Súbor} & \textbf{Formát} & \textbf{Obsah} \\ \hline
\texttt{config/access\_security.yaml} & YAML & Hlavný konfiguračný súbor so štyrmi sekciami: \texttt{frame\_handler} (anti-replay, max skew, AAD mód), \texttt{key\_management} (verzia kľúča, cesta k~master key), \texttt{storage} (cesta k~SQLite), \texttt{admin} (port, token, HMAC secret, decision mode). Sekcia \texttt{experiment} je len v~6~profilových YAML súboroch. \\ \hline
\texttt{secrets/master\_key.hex} & Text (hex) & 64 hexadecimálnych znakov (32~bajtov po dekódovaní). Zdieľané tajomstvo pre HKDF deriváciu všetkých podkľúčov. Nie je súčasťou priloženého archívu --- generuje sa jednorázovo pri prvom nasadení. \\ \hline
\texttt{hw\_layer/hw\_config.h} & C hlavička & Konfigurácia firmvéru: Wi-Fi prístupové údaje, URL servera, HMAC secret, identita čítačky, priradenie GPIO pinov, časové konštanty (popísané v~podkapitole~3.1). \\ \hline
\end{tabular}
\end{table}

\paragraph{Produkčné výstupy.}
Pri prevádzke systém produkuje: \texttt{data/access.db} --- SQLite databázu (WAL mód) so~6~tabuľkami (\texttt{cards}, \texttt{door\_roles}, \texttt{readers}, \texttt{reader\_doors}, \texttt{audit\_log} s~HMAC hash chain, \texttt{audit\_anchor} proti truncation; schéma v~tabuľke~\ref{t:db-schema}), automaticky vytvorenú cez \texttt{CREATE TABLE IF NOT EXISTS}; JSON HTTP odpovede vo formáte \texttt{\{"allow": bool, "reason": string\}} (HW endpoint) a~\texttt{\{"status": "ok"|"error", ...\}} (admin API, autentifikácia cez \texttt{X-Admin-Token}); voliteľný debug výstup na sériovej konzole ESP32 (UID, HMAC podpis, HTTP kód --- v~produkcii odporúčané vypnúť).

\paragraph{Vstupné súbory experimentov.}
Experimentálny harness nevyžaduje YAML konfiguráciu --- šesť profilov je definovaných programaticky vo funkcii \texttt{allProfiles()}, čím sa eliminuje závislosť na externých súboroch a~riziko nekonzistencie medzi konfiguráciou a~kódom. Produkčný \texttt{master\_key.hex} sa načíta len v~E2E režime (pri in-process behu harness používa deterministický kľúč \texttt{0x00..0x1F} pre reprodukovateľnosť). Všetky parametre behu sú riadené cez CLI prepínače uvedené v~tabuľke~\ref{t:exp-cli}.

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.15}
\caption{CLI prepínače experimentálneho harnessu}\label{t:exp-cli}
\begin{tabular}{|l|l|p{5.2cm}|} \hline
\textbf{Prepínač} & \textbf{Predvolená hodnota} & \textbf{Význam} \\ \hline
\texttt{--seed=N}     & 42    & Základ pre seed behov ($42 + \text{run\_idx}$). \\ \hline
\texttt{--warmup=N}   & 200   & Počet warmup frame (nezaznamenávaný). \\ \hline
\texttt{--baseline=N} & 200   & Počet baseline frame (validné, zaznamenávané). \\ \hline
\texttt{--runs=N}     & 5     & Počet opakovaní pre štatistickú robustnosť. \\ \hline
\texttt{--e2e=URL}    & ---   & E2E režim: frame sa posiela cez HTTP na bežiaci server. \\ \hline
\texttt{--profile=P}  & ---   & Filter na jeden profil (napr.\ \texttt{A1-R2}). \\ \hline
\end{tabular}
\end{table}

\noindent Adresár \texttt{config/} obsahuje aj šesť referenčných profilových YAML súborov (\texttt{profile\_a\{1,2\}\_r\{0,1,2\}.yaml}), ktoré dokumentujú kompletnú produkčnú konfiguráciu pre každú kombináciu AEAD algoritmu a~nonce režimu. Experimentálne skripty (\texttt{run\_e2e.sh}) generujú ekvivalentnú dočasnú konfiguráciu inline kvôli izolácii portov a~SQLite úložiska medzi behmi profilov.

\paragraph{Výstupné súbory experimentov.}
Osem binárnych scenárov (sedem útočných tried S1--S7 plus rozdelený S3 na S3a/S3b) produkuje osem CSV súborov s~názvami zhodnými s~binárkou (\texttt{s1\_replay.csv}, \texttt{s2\_tamper.csv}, \texttt{s3\_rng\_fault.csv}, \texttt{s3\_cross\_reader.csv}, \texttt{s4\_seq\_reset.csv}, \texttt{s5\_tag\_probe.csv}, \texttt{s6\_throughput.csv}, \texttt{s7\_nonce\_tamper.csv}) do adresára \texttt{build/experiments/results/} (in-process) alebo \texttt{build/experiments/results\_e2e/} (E2E režim). CSV formát bol zvolený pre priamu kompatibilitu s~analytickým pipeline (\texttt{pandas.read\_csv}) a~možnosť manuálnej inšpekcie v~tabuľkovom procesore.

\noindent Každý CSV súbor má jednotnú štruktúru s~15~stĺpcami: \texttt{profile}, \texttt{cipher\_mode}, \texttt{nonce\_mode}, \texttt{detector\_enabled} (konfigurácia behu); \texttt{scenario}, \texttt{phase} (\texttt{baseline}/\texttt{attack}/\texttt{measure}), \texttt{attack\_n}, \texttt{frame\_idx} (kontext); \texttt{allowed}, \texttt{reason} (kódy ako \texttt{ok}, \texttt{replay}, \texttt{quarantined}), \texttt{quarantined}, \texttt{anomaly\_type} (rozhodnutie); \texttt{latency\_ns} (meranie); \texttt{seed}, \texttt{run\_idx} (reprodukovateľnosť). Celkový objem dát pri štandardnom behu (5~behov, attack\_n~=~50\ldots500): ${\sim}2{,}977{,}500$ riadkov naprieč 8~CSV súbormi.

\paragraph{Výstupy analytického pipeline.}
Skript \texttt{analysis/analyze.py} načíta CSV súbory a~vygeneruje do adresára \texttt{analysis/plots/} dve kategórie výstupov --- sedem PNG grafov a~tri \LaTeX{} tabuľky --- zhrnuté v~tabuľke~\ref{t:analysis-outputs}.

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.15}
\caption{Výstupy analytického pipeline}\label{t:analysis-outputs}
\begin{tabular}{|l|l|p{4.8cm}|} \hline
\textbf{Súbor} & \textbf{Typ} & \textbf{Obsah} \\ \hline
\texttt{attack\_success\_heatmap.png}  & PNG (300\,DPI) & Heatmapa úspešnosti útokov (scenár $\times$ profil). \\ \hline
\texttt{attack\_success\_by\_n.png}    & PNG & Závislosť úspešnosti od počtu frame. \\ \hline
\texttt{quarantine\_speed.png}         & PNG & Počet frame do prvej karantény (R2). \\ \hline
\texttt{latency\_boxplot.png}          & PNG & Distribúcia latencie (baseline vs.\ attack). \\ \hline
\texttt{throughput.png}                & PNG & Priepustnosť a~percentily latencie (S6). \\ \hline
\texttt{detector\_overhead.png}        & PNG & Porovnanie R1 vs.\ R2 na baseline. \\ \hline
\texttt{scenario\_summary\_table.png}  & PNG & Súhrnná tabuľka výsledkov. \\ \hline
\texttt{scenario\_summary.tex}         & \LaTeX{} & Tabuľka výsledkov (vkladaná cez \texttt{\textbackslash input}). \\ \hline
\texttt{throughput\_table.tex}         & \LaTeX{} & Výkonnostné metriky (FPS, percentily). \\ \hline
\texttt{baseline\_correctness.tex}     & \LaTeX{} & False reject rate na baseline. \\ \hline
\end{tabular}
\end{table}

\noindent LaTeX tabuľky sú generované z~rovnakých dát ako grafy a~vkladajú sa priamo do textu práce cez \texttt{\textbackslash input\{plots/...\}}, čím je zaručená konzistencia numerických hodnôt naprieč grafmi a~tabuľkami.

\subsection{Popis správ pre systémového programátora}

\paragraph{Serverová aplikácia.}
Server vypisuje na štandardný výstup prevádzkové správy:

\begin{itemize}
\item \texttt{Admin server: http://0.0.0.0:8080} --- frontend služba úspešne nasadená.
\item \texttt{Decision service: http://127.0.0.1:8081} --- DecisionService úspešne nasadený.
\item \texttt{listen failed} --- nepodarilo sa naviazať port (typicky obsadený iným procesom alebo nedostatočné oprávnenia).
\item \texttt{[decision] exception: <popis>} --- výnimka pri spracovaní frame; frame je zamietnutý s~dôvodom \texttt{internal\_error}, server pokračuje v~prevádzke.
\end{itemize}

\noindent Detailné prevádzkové udalosti (rozhodnutia o~prístupe, detekované anomálie, aktivácie karantény) nie sú vypisované na štandardný výstup --- sú prístupné cez webové administrátorské rozhranie v~sekciách \emph{Runtime logs} (polling 500\,ms) a~\emph{Audit} (auto-refresh 1\,s). Chybové kódy vrátené v~JSON odpovediach na HW endpoint (\texttt{reason}: \texttt{ok}, \texttt{replay}, \texttt{decrypt\_failed}, \texttt{unknown\_reader}, \texttt{unknown\_card}, \texttt{forbidden}, \texttt{quarantined}, \texttt{bad\_payload}, \texttt{bad\_action}, \texttt{remote\_unavailable}, \texttt{internal\_error}) sú popísané v~používateľskej príručke.

\paragraph{Firmvér ESP32.}
Firmvér vypisuje na sériovú konzolu (115\,200~baud) správy pri každej fáze spracovania: inicializácia (\texttt{hw\_seq loaded}, \texttt{RC522 ready}), pripojenie k~Wi-Fi (\texttt{Connecting...}, \texttt{OK}/\texttt{FAILED}), detekcia karty (\texttt{UID: <hex>}), odoslanie požiadavky (\texttt{POST <URL>}, \texttt{Body: ...}, \texttt{Sig: ...}), odpoveď servera (\texttt{HTTP <kód>}, \texttt{Resp: ...}) a~finálne rozhodnutie (\texttt{ALLOW}/\texttt{DENY}/\texttt{SEND FAILED}). Chybové stavy: \texttt{WiFi not connected} (zlyhanie Wi-Fi), \texttt{Skip (too soon)} (debounce 1500\,ms), \texttt{Bad response JSON (no allow field)} (neplatná odpoveď servera). Sériový výstup obsahuje citlivé údaje (UID, HMAC podpis) --- v~produkčnom nasadení sa odporúča vypnúť.

\section{Preklad programu}

Systém sa skladá z~troch nezávisle zostavovaných častí: serverovej aplikácie (C++20, CMake), firmvéru čítačky (Arduino/ESP32) a~analytického pipeline (Python). Táto kapitola opisuje štruktúru prekladového procesu, potrebné závislosti a~spôsob zostavenia každej časti. Konkrétne príkazy na zostavenie a~spustenie sú uvedené v~používateľskej príručke.

\paragraph{Serverová aplikácia (C++20).}
Koreňový \texttt{CMakeLists.txt} so štandardom C++20 registruje deväť produkčných modulov (\texttt{crypto\_lib} až \texttt{access\_admin}) a~modul \texttt{experiments}. Každý modul je statická knižnica; \texttt{access\_admin} navyše produkuje hlavný binárny súbor servera, scenáre v~\texttt{experiments/} sú samostatné spustiteľné programy. CMake FetchContent pri prvom zostavení automaticky stiahne 5 externých knižníc (konfigurácia v~\texttt{cmake\_other/third\_party.cmake}). Predpoklady a~verzie sú v~tabuľke~\ref{t:sw-req}. Verifikácia: \texttt{ctest} (44 testov, voliteľne ASan/UBSan cez CMake príznaky).

\paragraph{Docker nasadenie.}
Multi-stage \texttt{Dockerfile}: \textbf{builder} (\texttt{ubuntu:24.04} + \texttt{build-essential}, \texttt{cmake}, \texttt{g++}, \texttt{libsqlite3-dev}, \texttt{python3}, \texttt{git}) skompiluje projekt a~spustí testy; \textbf{runtime} (\texttt{ubuntu:24.04} + \texttt{libsqlite3-0}) obsahuje len binárny súbor servera, web rozhranie a~konfiguráciu. Adresáre \texttt{data/} a~\texttt{secrets/} sa pri spustení pripájajú ako bind mounts (\texttt{-v}).

\paragraph{Firmvér ESP32 a~Python pipeline.}
Firmvér sa kompiluje v~Arduino IDE 2.x (vyžaduje Espressif Arduino Core a~knižnicu MFRC522, úpravu \texttt{hw\_config.h} a~nahratie cez USB). Analytický pipeline vyžaduje Python $\geq$~3.10 a~knižnice z~\texttt{analysis/requirements.txt}; vstupom je skript \texttt{analyze.py}, výstupom PNG grafy a~LaTeX tabuľky v~\texttt{analysis/plots/} (tabuľka~\ref{t:analysis-outputs}).

\subsection{Zoznam zdrojových textov}

Kompletné zdrojové texty sú súčasťou priloženého média. Tabuľka~\ref{t:src-server} sumarizuje moduly serverovej aplikácie; ostatné komponenty (firmvér, web, experimenty, analýza) sú zhrnuté v~nasledujúcom odseku.

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.12}
\caption{Zdrojové súbory serverovej aplikácie (C++20) podľa modulov}\label{t:src-server}
\begin{tabular}{|l|c|c|c|p{3.6cm}|} \hline
\textbf{Modul} & \textbf{.hpp} & \textbf{.cpp} & \textbf{Testy} & \textbf{Hlavná zodpovednosť} \\ \hline
\texttt{crypto\_lib}       & 4 & 4 & 2 & AEAD, HKDF, HMAC, nonce generátory \\ \hline
\texttt{protocol\_lib}     & 5 & 5 & 2 & Binárny formát frame, replay window \\ \hline
\texttt{key\_manager}      & 2 & 2 & 0 & HKDF derivácia, hex utility \\ \hline
\texttt{access\_core}      & 3 & 3 & 2 & FrameHandler, anomálny detektor \\ \hline
\texttt{access\_decision}  & 6 & 4 & 2 & DecisionEngine, RBAC, CardIdHasher \\ \hline
\texttt{access\_storage}   & 5 & 4 & 2 & SQLite, audit log, verifikácia chain \\ \hline
\texttt{config\_loader}    & 1 & 1 & 0 & Načítanie YAML konfigurácie \\ \hline
\texttt{runtime\_events}   & 1 & 1 & 0 & EventBus (udalosti pre web) \\ \hline
\texttt{access\_admin}     & 12 & 13 & 0 & HTTP server, služby, \texttt{main()} \\ \hline
\multicolumn{1}{|r|}{\textbf{Spolu}} & \textbf{39} & \textbf{37} & \textbf{10} & 76~zdrojových + 10~testovacích súborov \\ \hline
\end{tabular}
\end{table}

\noindent Ostatné komponenty: \texttt{experiments/} --- 20~súborov C++ (6~spoločných + 8~scenárov + 6~hlavičiek); \texttt{hw\_layer/} --- 2~súbory firmvéru ESP32 (\texttt{.ino}, \texttt{.h}); \texttt{web/} --- 10~súborov frontendu (HTML, 8~JS, CSS); \texttt{analysis/} --- 1~Python skript (\texttt{analyze.py}); \texttt{config/} --- 7~YAML konfigurácií (1~produkčná + 6~profilov); 16~\texttt{CMakeLists.txt}, \texttt{Dockerfile} a~\texttt{run\_e2e.sh}.

\subsection{Požiadavky na technické prostriedky pri preklade}

Táto podkapitola uvádza požiadavky na hardvér; požiadavky na programové vybavenie sú v~podkapitole~4.3. Tabuľka~\ref{t:hw-req} sumarizuje minimálne a~odporúčané parametre.

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.15}
\caption{Požiadavky na technické prostriedky}\label{t:hw-req}
\begin{tabular}{|l|p{4.5cm}|p{4.5cm}|} \hline
\textbf{Parameter} & \textbf{Preklad (build)} & \textbf{Prevádzka (runtime)} \\ \hline
Procesor & x86-64 alebo ARM64; min.\ 2 jadrá (4+ pre paralelný preklad) & x86-64 alebo ARM64; 1+ jadro \\ \hline
Operačná pamäť & Min.\ 2\,GB (4\,GB odporúčané) & Min.\ 128\,MB \\ \hline
Diskový priestor & ${\sim}1$\,GB (zdrojový kód 35\,MB + závislosti 325\,MB + build 500\,MB) & ${\sim}150$\,MB (Docker obraz 123\,MB) \\ \hline
Sieťové pripojenie & Áno (prvé zostavenie stiahne závislosti) & Wi-Fi pre komunikáciu s~čítačkou \\ \hline
Prídavné zariadenia & --- & ESP32 DevKit + MFRC522 + USB kábel \\ \hline
\end{tabular}
\end{table}

\noindent Pre preklad firmvéru ESP32 postačuje počítač s~${\sim}1$\,GB voľného miesta (Arduino IDE + ESP32 toolchain) a~USB portom. Analytický pipeline (Python) vyžaduje ${\sim}200$\,MB pre virtuálne prostredie a~min.\ 1\,GB RAM pre spracovanie ${\sim}3$M riadkov CSV. Pri použití Docker nasadenia sú požiadavky na preklad aj prevádzku pokryté kontajnerom --- stačí nainštalovaný Docker Engine.

\subsection{Požiadavky na programové prostriedky pri preklade}

Tabuľka~\ref{t:sw-req} sumarizuje programové vybavenie potrebné pre zostavenie a~prevádzku jednotlivých častí systému.

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.15}
\caption{Požiadavky na programové prostriedky}\label{t:sw-req}
\begin{tabular}{|l|p{5.5cm}|l|} \hline
\textbf{Komponent} & \textbf{Nástroj / knižnica} & \textbf{Verzia} \\ \hline
\multicolumn{3}{|l|}{\emph{Serverová aplikácia (preklad)}} \\ \hline
Operačný systém      & Linux (Ubuntu 22.04+) & --- \\ \hline
C++ kompilátor       & GCC (podpora C++20) & $\geq$ 12 \\ \hline
Build systém         & CMake & $\geq$ 3.20 \\ \hline
Databáza (hlavičky)  & SQLite3 (\texttt{libsqlite3-dev}) & 3.x \\ \hline
Závislosti (auto)    & libsodium, nlohmann/json, yaml-cpp, cpp-httplib, Google Test & FetchContent \\ \hline
\multicolumn{3}{|l|}{\emph{Serverová aplikácia (prevádzka)}} \\ \hline
Operačný systém      & Linux (Ubuntu 22.04+); Docker & --- \\ \hline
Databáza (runtime)   & SQLite3 (\texttt{libsqlite3-0}) & 3.x \\ \hline
Kontajner (voliteľ.) & Docker Engine & 20.10+ \\ \hline
\multicolumn{3}{|l|}{\emph{Firmvér čítačky}} \\ \hline
Vývojové prostredie  & Arduino IDE (Linux) & 2.x \\ \hline
Podpora platformy    & Espressif Arduino Core (ESP32) & 3.x \\ \hline
Knižnica RFID        & MFRC522 (Miguel Balboa) & 1.4.x \\ \hline
\multicolumn{3}{|l|}{\emph{Analytický pipeline}} \\ \hline
Interpreter          & Python & $\geq$ 3.10 \\ \hline
Knižnice             & pandas, matplotlib, seaborn, numpy & \texttt{req.txt} \\ \hline
\end{tabular}
\end{table}

\noindent Všetky C++ závislosti servera sa získavajú automaticky cez \texttt{CMake FetchContent}; nie je potrebný žiadny externý správca balíkov. Python závislosti sa inštalujú cez \texttt{pip} z~priloženého \texttt{requirements.txt}.

\section{Nadväznosť na iné programové produkty}

Program \emph{Access-Security} stojí na viacerých externých open-source produktoch s~permisívnymi licenciami (ISC, MIT, BSD, Apache~2.0). Tabuľka~\ref{t:ext-deps} sumarizuje ich rolu v~systéme; konkrétne verzie sú uvedené v~tabuľke~\ref{t:sw-req} (podkapitola~4.3).

\begin{table}[!htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.12}
\caption{Externé produkty a~ich rola v~systéme}\label{t:ext-deps}
\begin{tabular}{|l|p{4.0cm}|p{4.0cm}|} \hline
\textbf{Produkt} & \textbf{Rola} & \textbf{Spôsob integrácie} \\ \hline
\multicolumn{3}{|l|}{\emph{Server (C++20)}} \\ \hline
libsodium      & AEAD, HMAC, HKDF, randombytes, constant-time compare & FetchContent (zdrojový kód) \\ \hline
SQLite3        & Relačná DB (WAL) pre čítačky, karty, audit & Systémová knižnica \\ \hline
cpp-httplib    & HTTP server (frontend + DecisionService) & Header-only, FetchContent \\ \hline
nlohmann/json  & JSON parsovanie/serializácia & Header-only, FetchContent \\ \hline
yaml-cpp       & Načítanie YAML konfigurácie & Statická, FetchContent \\ \hline
Google Test    & Unit testy (44 prípadov) & Statická, FetchContent \\ \hline
\multicolumn{3}{|l|}{\emph{Firmvér ESP32}} \\ \hline
Arduino Core   & HAL: Wi-Fi, NVS, GPIO, SPI, LEDC & Arduino Board Manager \\ \hline
MFRC522        & Ovládač RFID čítačky (ISO~14443) & Arduino Library Manager \\ \hline
mbedTLS        & HMAC-SHA-256 pre autentifikáciu & Súčasť ESP-IDF \\ \hline
\multicolumn{3}{|l|}{\emph{Analytický pipeline a~nasadenie}} \\ \hline
pandas, numpy, matplotlib, seaborn & Spracovanie CSV, grafy, percentily & \texttt{pip} (\texttt{requirements.txt}) \\ \hline
Docker Engine  & Kontajnerizácia (multi-stage) & Systémový balík \\ \hline
\end{tabular}
\end{table}

\noindent Kryptografická knižnica libsodium\cite{LibsodiumDoc} prešla nezávislým bezpečnostným auditom\cite{LibsodiumAudit} a~všetky jej operácie sú implementované v~konštantnom čase\cite{Bernstein2012}. Na ESP32 je mbedTLS jedinou dostupnou implementáciou HMAC v~rámci Arduino Core.

\section{Zhodnotenie riešenia}

Implementácia spĺňa hlavný cieľ práce --- funkčný prototyp prístupového systému s~viacvrstvovou kryptografickou ochranou, experimentálne overený v~siedmich scenároch. Nasledujúce body sumarizujú aktuálne obmedzenia, nedopracované oblasti a~potenciálne smery ďalšieho vývoja.

\paragraph{Obmedzenia produkčného nasadenia.}
Komunikácia prebieha cez nešifrovaný HTTP --- HMAC zabezpečuje integritu a~autentifikáciu, ale nie dôvernosť (pasívny pozorovateľ vidí UID); nasadenie mTLS\cite{RFC8446} by túto medzeru uzavrelo. Master kľúč je v~hex súbore a~HMAC secret firmvéru je vkompilovaný do binárky --- pre produkciu vhodnejšie HSM alebo vault\cite{NIST80057}. Porovnanie admin tokenu nie je konštantnočasové (teoretický timing side-channel pri krátkych tokenoch). Karta slúži ako identifikátor, nie kryptografický dôkaz identity --- ochrana proti klonovaniu je len na serverovej strane (anti-replay, anomálny detektor); migrácia na MIFARE DESFire\cite{GarciaMIFARE2008} by túto vrstvu posilnila.

\paragraph{Nedopracované oblasti.}
Automatická rotácia kľúčov podľa časového plánu (manuálna cez \texttt{key\_version} podporovaná). Offline režim na čítačke (aktuálne fail-closed pri výpadku servera). Sériový debug výstup ESP32 vypisuje citlivé údaje --- v~produkcii je potrebné vypnúť. Karanténa nemá voliteľný časový limit pre automatické zrušenie.

\paragraph{Potenciálne smery ďalšieho vývoja.}
Geograficky distribuované nasadenie cez synchronizáciu stavu (replay window, karanténa) medzi inštanciami DecisionService. Adaptívna úprava prahov detektora (\texttt{rollbackThreshold}, \texttt{tagFailStreakLimit}) podľa štatistík prevádzky. Rozšírenie experimentálnej sady o~adaptívneho útočníka, ktorý striedaním platných a~neplatných frame obchádza \texttt{tag\_fail\_streak}. Doplnenie webového rozhrania o~autentifikáciu používateľov, auditovanie admin akcií a~HTTPS.

\paragraph{Celkové zhodnotenie.}
Napriek uvedeným obmedzeniam prototyp demonštruje, že kombinácia štandardných kryptografických primitív (ChaCha20-Poly1305\cite{RFC8439}, HMAC-SHA-256\cite{FIPS1981}, HKDF\cite{RFC5869}) s~premyslenými architektonickými rozhodnutiami (deterministický nonce, runtime anomálna detekcia, per-reader derivácia kľúčov, tamper-evident audit) poskytuje merateľne vyššiu odolnosť voči testovaným triedam útokov v~porovnaní so štandardným nasadením AEAD\@. Obmedzenia sú väčšinou dôsledkom vedomého zúženia scope na účely diplomovej práce a~sú jasne dokumentované, čo umožňuje ich adresovanie v~budúcich iteráciách.

\bibliography{bibliography}

\section{Zoznam príloh}

Priložené elektronické médium obsahuje zdrojový archív projektu so štruktúrou: deväť produkčných modulov (\texttt{crypto\_lib}, \texttt{protocol\_lib}, \texttt{key\_manager}, \texttt{access\_core}, \texttt{access\_decision}, \texttt{access\_storage}, \texttt{config\_loader}, \texttt{runtime\_events}, \texttt{access\_admin} --- popísané v~tabuľke~\ref{t:src-server}), \texttt{hw\_layer/} (firmvér ESP32), \texttt{web/} (webové rozhranie), \texttt{experiments/} (C++ harness), \texttt{analysis/} (Python pipeline), \texttt{config/} (YAML konfigurácie), \texttt{cmake\_other/} (CMake FetchContent), \texttt{Dockerfile}, koreňový \texttt{CMakeLists.txt} a~adresár \texttt{tukedip\_output/} s~textom diplomovej práce a~systémovej príručky v~\LaTeX{}.

Každý modul má podadresár \texttt{include/} (verejné hlavičky), \texttt{src/} (implementácia) a~voliteľne \texttt{tests/} (unit testy). Zdrojové súbory používajú Doxygen-štýl komentárov (\texttt{//!}, \texttt{@param}, \texttt{@return}); identifikácia autora a~projektu je uvedená v~koreňovom \texttt{README.md} a~v~hlavičkách jednotlivých zdrojových súborov. Pre minimálnu produkčnú distribúciu postačuje odstrániť adresáre \texttt{experiments/}, \texttt{analysis/} a~\texttt{tukedip\_output/} --- produkčný kód je voči nim nezávislý a~server sa zostaví a~spustí aj bez nich.

\end{document}